---
import type { SkillGroup } from "../../data";
import Badge from "./../ui/Badge.astro";

export interface Props {
  data: SkillGroup[];
}

const { data } = Astro.props;
---

<section
  class="flex items-start justify-between flex-col sm:flex-row dark:text-gray-100 mt-16"
  id="certification"
>
  <h2
    class="w-[15rem] font-montserrat text-slate-900 dark:text-slate-200 underline underline-offset-4"
  >
    Skills
  </h2>
  <div class="w-full flex flex-col gap-4">
    {
      data.map((skillGroup) => (
        <div>
          <h3 class="text-md font-semibold font-montserrat">
            {skillGroup.category}
          </h3>
          <div class="flex flex-row gap-2 flex-wrap mt-4">
            {skillGroup.skills.map((v) => (
              <Badge content={v.name} />
            ))}
          </div>
        </div>
      ))
    }
  </div>
</section>
